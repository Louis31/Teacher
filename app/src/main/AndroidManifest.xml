<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.haiku.wateroffer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:name=".APPApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"

        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".module.common.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".module.common.MainActivity" />
        <!-- 订单相关 -->
        <activity android:name=".module.order.OrderDetailActivity" />
        <!-- 商品相关 -->
        <activity android:name=".module.goods.GoodsEditActivity" />
        <!-- 店铺相关 -->
        <activity android:name=".module.shop.ShopNameActivity" />
        <activity android:name=".module.shop.ShopAddressActivity" />
        <activity android:name=".module.shop.EditPhoneActivity" />
        <activity android:name=".module.shop.DeliverListActivity" />
        <activity android:name=".module.shop.MyBillActivity" />
        <activity android:name=".module.shop.DepositActivity" />
        <activity android:name=".module.shop.ContributionActivity" />

    </application>

</manifest>
